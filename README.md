# Bun Hono for gmi

- Use Bun as JavaScript Runtime
- Use Hono as backend framework
- Use Vite as builder for React frontend: bun create vite
- Use Tanstack as Meta Framework

To install dependencies:

```sh
bun install
```

To run:

```sh
bun run dev
```

open http://localhost:3000
